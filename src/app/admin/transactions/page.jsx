'use client';
import React from 'react';
import Transactions from '../UI/Trx/Transactions';

const page = () => {
    return (
        <div>
            <Transactions />
        </div>
    );
};

export default page;